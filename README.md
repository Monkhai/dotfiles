# dotfiles

My personal dotfiles.

## Setup

```
git clone git@github.com:YOUR_USERNAME/dotfiles.git ~/dotfiles
cd ~/dotfiles
./install.sh
```

This symlinks config files to their expected locations so edits in either place stay in sync.
